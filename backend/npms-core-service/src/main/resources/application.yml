server:
  port: 8083
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:npms_db}
    username: ${DB_USER:npms_user}
    password: ${DB_PASSWORD:npms_local_pass_2026}
  jpa:
    hibernate.ddl-auto: none
    properties.hibernate.default_schema: public
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    schemas: public
    baseline-on-migrate: true
    baseline-version: 0
    out-of-order: false
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP:localhost:9092}
  mail:
    host: ${SPRING_MAIL_HOST:smtp.gmail.com}
    port: ${SPRING_MAIL_PORT:587}
    username: ${SPRING_MAIL_USERNAME:sixer3080@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:fcbpwjknaqytivzc}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
jwt:
  public-key-path: ${JWT_PUBLIC_KEY_PATH:classpath:keys/public.pem}

management:
  health:
    redis:
      enabled: false
    kafka:
      enabled: false

